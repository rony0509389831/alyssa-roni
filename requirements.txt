# ==========================================
# תשתית נתונים וממשק (Core & UI)
# ==========================================
pandas==3.0.*
numpy==2.4.*
streamlit==1.57.*
requests==2.34.*

# ==========================================
# מיפוי ו-GIS (Geospatial Stack)
# ==========================================
geopandas==1.1.*
folium==0.20.*
streamlit-folium==0.27.*
pyarrow==24.0.*
pyogrio==0.12.*

# ==========================================
# ויזואליזציה (Visualization)
# ==========================================
matplotlib==3.10.*

# ==========================================
# מדע נתונים (ML & Data Science)
# ==========================================
scikit-learn==1.8.*
joblib>=1.3            # שמירה/טעינה של מודל ה-TCI המאומן (data/tci_model.joblib)

# ==========================================
# ניווט ושמש (M3 + מפת דמו)
# ==========================================
osmnx>=1.9            # גרף רחובות OSM + geocoding
networkx>=3.3         # Dijkstra + graph operations
pysolar>=0.11         # נדרש לחישוב מיקום השמש במפה
tzdata>=2024.1        # נדרש ל-ZoneInfo("Asia/Jerusalem") ב-Linux/Streamlit Cloud
shapely>=2.0          # מצולעי-צל, STRtree — precompute_shadow.py (בעבר תלות-עקיפה בלבד של geopandas)
scipy>=1.11           # cKDTree — footprint לפי צפיפות מקומית ב-precompute_shadow.py

# ==========================================
# סוכן LLM (M4)
# ==========================================
groq>=0.11            # לקוח Groq (תואם-OpenAI) לחילוץ פרמטרי ניווט מטקסט חופשי

# ==========================================
# בדיקות קוד (Testing)
# ==========================================
pytest>=8.2
