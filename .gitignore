# ==========================================
# קבצי נתונים ו-GIS (Data & Geospatial Files)
# ==========================================
# הערה: data/tel_aviv_walk.graphml *כן* מועלה ל-repo (24MB) — חובה שה-deploy
# ישתמש באותו גרף שממנו נבנו edges_features.parquet ו-shadow_coverage.parquet.
# אחרת load_graph() מוריד גרף OSM טרי שלא תואם → מקטעי "TCI לא זמין" במסלול.
tel_aviv_buildings.geojson
*.geojson
*.shp
*.dbf
*.prj
*.shx
*.kml
*.xlsx
*.csv
!buildings_clean.csv

# ==========================================
# קבצי סקריפט והורדה מקומיים (Local Scratch Scripts)
# ==========================================
download_buildings.py
check_data.py
Explanation_files.txt
clean_buildings.py
eda_buildings.py
outputs_M2/
SESSION_SUMMARY.md

# ==========================================
# קבצי פייתון זמניים וסביבות וירטואליות (Python & Envs)
# ==========================================
__pycache__/
*.pyc
*.pyo
*.pyd
.ipynb_checkpoints/

# סביבות וירטואליות (אם תפתחו מקומית במחשב)
.venv/
venv/
env/
ENV/

# קבצי הגדרות של מערכת ההפעלה או VS Code
.vscode/
.DS_Store
Thumbs.db

# cache של OSMnx (geocoding) — נוצר אוטומטית, לא שייך ל-repo
cache/

# ==========================================
# סודות (M4 — מפתח Groq) — לעולם לא ל-repo
# ==========================================
.streamlit/secrets.toml

# ==========================================
# גיבויי-בדיקה זמניים (נוצרים ידנית לצורך השוואת before/after, לא חלק מה-repo)
# ==========================================
data/*_backup.parquet
